# AUSF -- servidor de autenticacao. Participa no 5G-AKA durante o registo.
logger:
  level: info

global:

ausf:
  sbi:
    server:
      - address: 10.55.0.20
        port: 7777
    client:
      scp:
        - uri: http://10.55.0.11:7777
  # time.nf_instance.heartbeat fica no valor por omissao (10 s). A experiencia que
  # mediu o preco de um intervalo longo esta em
  # results/summaries/2026-09-04-heartbeats.md
